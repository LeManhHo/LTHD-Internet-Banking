/*
 * IBanking Messages
 *
 * This contains all the text for the IBanking container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IBanking';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IBanking container!',
  },
});
